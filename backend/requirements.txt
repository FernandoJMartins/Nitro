fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic-settings==2.7.1
python-multipart==0.0.20
Pillow>=12.2.0,<13
openai>=1.55,<2
bcrypt==4.2.1
pyjwt==2.10.1
requests>=2.34.2,<3
PySocks==1.7.1
# automação não-oficial do Instagram (sessão real, proxy SOCKS5) — NÃO é a Graph API oficial da Meta
# Fixado no commit master 09c96e4 (2026-09-12, release 3.0.1): login CAA por padrão com
# transporte curl_cffi (HTTP/2 + TLS híbrido). O transporte requests antigo leva 429 no
# CAA/bloks, e o login legado foi descontinuado pelo Instagram (needs_upgrade em todas as versões).
instagrapi @ https://codeload.github.com/subzeroid/instagrapi/tar.gz/09c96e40ee7c33b4b69506d4d36a0d86a5e5f6f2
# transporte privado do fork 3.x (fingerprint de navegador no login CAA) — explicitado
# aqui porque o login depende dele diretamente
curl_cffi>=0.15.0,<1
cryptography==43.0.3
